{
  "author": "Junbo Xiong",
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.1041.0",
    "@aws-sdk/client-lambda": "^3.1041.0",
    "@aws-sdk/client-s3": "^3.1041.0",
    "@aws/durable-execution-sdk-js": "^1.1.2",
    "typescript": "^6.0.3"
  },
  "license": "MIT",
  "main": "index.js",
  "name": "aws-sdk-local-fakes",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git@github.com:C6H5-NO2/aws-sdk-local-fakes.git"
  },
  "scripts": {
    "build": "npx tsc --build --stopBuildOnErrors",
    "clean": "npx tsc --build --clean && npm run clean --workspaces --if-present"
  },
  "type": "module",
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ]
}